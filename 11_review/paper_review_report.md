# 论文审查报告（PR-20260723-01）

## 审查结论

**结论：需要退回 ChatGPT 修订，当前不得进入 `finalize`。**

本轮审查对象为 `09_paper/full_draft.md`，其内容来自交接 `H-50C6A5E862DE`，并以冻结包 `RF-20260722T114756Z`、合同总线和可复现输出为唯一事实依据。审查没有改变任何模型假设、路线或数值结论。

## 已通过的核验

- `result_contract.csv` 中的 5 个唯一冻结数值均在论文中出现；未发现论文数值与冻结合同不一致的情况。
- 论文引用了 9 张正式图，全部可由 `09_paper/` 相对路径解析到 `08_figures/main_figures/` 中的已登记文件。
- Q1--Q5 的核心结论、Q3/Q4 未观察到新增协同收益的表述，以及“不宣称全局最优”的边界，均与冻结结果和已登记论断一致。
- 对 `13_prior_db/fulltext_index/source_texts/` 进行了独立的只读 8 字符 n-gram 扫描，最大重合率为 `0.000614`，低于 `0.08` 的复核阈值；详见 `11_review/paper_review_copy_risk_report.md`。
- 原始交接稿中的图路径不相对于 `09_paper/` 有效。Codex 仅将 9 处图路径改为指向既有、已登记的正式图文件；未改变图、数据、图注、数值或论文结论。

## 必须修订的问题

| 编号 | 严重性 | 发现 | 依据 | 处置 |
|---|---|---|---|---|
| PRR-001 | fail | 没有文内引文或参考文献，`citation_contract.csv` 只有表头。 | `paper_review` 阶段要求至少 1 条引用合同数据；合同要求引文元数据与支持等级可核验。 | 由 ChatGPT 添加真实、已核验且已阅读的来源，并同步登记合同。 |
| PRR-002 | major | 论文第 4.2 节使用 `d_{ikjq}(t)` 作为视线距离，但 FML007/FML008 登记的是 `delta_{ikjq}(t)`。 | `14_contracts/formula_contract.csv` 的 FML007、FML008。 | 由 ChatGPT 令论文中的符号、定义和遮蔽条件与已冻结公式合同逐字一致。 |
| PRR-003 | major | 论文的多弹并集定义漏写 `y_{ik}=1` 的使用条件。 | FML010：`U_j = union_{i,k: y_ik=1} I_ikj`。 | 由 ChatGPT 按 FML010 恢复使用条件，不能依赖未声明的“未使用弹区间自动为空”约定。 |
| PRR-004 | major | Q5 字典序说明出现“规定近优容差内”的新口径。 | FML013 是严格的字典序目标，冻结模型中未登记近优容差。 | 删除该表述并按 FML013 写明严格字典序；若必须引入容差，应退回 `model_design` 并重新走人工模型冻结。 |

上述问题已同步登记到 `11_review/revision_tasks.csv` 和 `14_contracts/revision_tasks.csv`。在问题关闭、论文重新导入并通过复审前，任何人工闸门均不应确认。

## 复审要求

1. ChatGPT 应在同一交接协议下提交修订稿，不得覆盖本次原始回复。
2. Codex 将复核数值、公式、图表、引用合同、修订任务关闭证据及全文一致性。
3. 只有所有 `fail` / `major` 修订任务关闭，且分数达到合同阈值后，才可请求 `paper_gate` 的人工确认。
