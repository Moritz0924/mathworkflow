# Stage Prompt Files

This directory contains one prompt contract per workflow stage.

Naming convention:

```text
<stage_order>_<stage_id>.md
```

Parent specification:

```text
prompts/stage_prompt_contract.md
```

These files are design and execution guidance only. They do not change Python behavior until a later task explicitly wires them into scripts.
