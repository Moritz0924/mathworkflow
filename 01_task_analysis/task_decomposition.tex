\section{问题分析}

本文首先对赛题进行分问拆解。以下内容由工作流自动生成，具体任务边界仍需人工复核。

\subsection{Q1 任务分析}
该问题初步判断为分类识别型，候选模型族为机器学习。候选主模型为随机森林/聚类/分类模型。该判断仍需结合数据字段和题目约束进行人工复核。

\subsection{Q2 任务分析}
该问题初步判断为预测分析型，候选模型族为预测回归。候选主模型为回归/时间序列模型。该判断仍需结合数据字段和题目约束进行人工复核。

\subsection{Q3 任务分析}
该问题初步判断为预测分析型，候选模型族为预测回归。候选主模型为回归/时间序列模型。该判断仍需结合数据字段和题目约束进行人工复核。

\subsection{Q4 任务分析}
该问题初步判断为预测分析型，候选模型族为预测回归。候选主模型为回归/时间序列模型。该判断仍需结合数据字段和题目约束进行人工复核。

\subsection{Q5 任务分析}
该问题初步判断为预测分析型，候选模型族为预测回归。候选主模型为回归/时间序列模型。该判断仍需结合数据字段和题目约束进行人工复核。

\subsection{Q6 任务分析}
该问题初步判断为预测分析型，候选模型族为预测回归。候选主模型为回归/时间序列模型。该判断仍需结合数据字段和题目约束进行人工复核。

